def qsort(array):
    for i in range(len(array)):
        for j in range(len(array) - i - 1):
            if array[j] > array[j + 1]:
                array[j], array[j + 1] = array[j + 1], array[j]
    return array


def BinarySearch(L, element):
    first = 0
    last = len(L)
    while first < last:
        mid = (first+last)//2
        if element>L[mid]:
            first = mid+1
        else:
            last = mid
    return first-1 if 0 < first < len(L) else "Такого числа нет в диапазоне списка"






mass = input('Введите 10 чисел через пробел: ')
mass = mass.split()
for i in range(len(mass)):
    try:
        mass[i] = int(mass[i])
    except ValueError:
        print ('Ошибка, Вы ввели не цифру!')
        exit()
if len(mass) != 10:
    print ('Вы ввели не 10 цифр.')
    exit()

number = input('Введите любое число: ')
try:
    number = int(number)
except ValueError:
    print ('Ошибка, Вы ввели не число! ')
    exit()

mass = qsort(mass)
print (mass)

print(BinarySearch(mass, number))